import React from "react";
import UserInfo from "./UserInfo";

const Profile = () => {
    // console.log(user?.photoURL);
    return (
        <div>
            <UserInfo />
        </div>
    );
};

export default Profile;
